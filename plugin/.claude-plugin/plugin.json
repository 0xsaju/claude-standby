{
  "name": "claude-auto-resume",
  "version": "0.5.0",
  "description": "Makes Claude Code tasks survive rate-limit hits: detects the limit, waits until reset, and auto-resumes the session with proper context, graded by task importance."
}
